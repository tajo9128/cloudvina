# AI Models Directory

Upload your trained `.pkl` files here.

Expected file: `rf_v3.0_production.pkl` (or `rf_model.pkl`)
Expected card: `model_card.json`

The `RFModelService` will automatically detect and load these files on startup.
