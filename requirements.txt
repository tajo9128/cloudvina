# Core FastAPI
fastapi==0.109.0
uvicorn==0.27.0
python-multipart

# ML - Lightweight versions
scikit-learn>=1.4.0
pandas>=2.1.4
numpy>=1.26.3
joblib>=1.3.2

# For SMILES validation (lighter than rdkit)
# Note: rdkit-pypi is large, using basic validation for now

# HuggingFace
huggingface_hub>=0.20.0
transformers>=4.36.0
sentencepiece>=0.1.99

# Auth & DB
python-jose[cryptography]
pydantic
pydantic-settings
python-dotenv>=1.0.0

# Optional - comment out if causing issues
# torch  # Will use CPU-only transformers
# rdkit-pypi  # Heavy, enable later if needed
